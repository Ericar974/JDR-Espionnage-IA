/*
 * Interface for API error responses, standardizing error messaging across the application.
 */
export interface GetApiError {
  // Descriptive message detailing the nature of the error encountered.
  message: string;
}
